import { handleActions } from 'redux-actions';
import {} from '../actions/bitcoinActions';

const initialState = {};

const authHandler = {};

const bitcoinReducer = handleActions(authHandler, initialState);
export default bitcoinReducer;
